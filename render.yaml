services:
  - type: web
    name: risc-v-sim
    runtime: docker
    plan: free
    region: singapore
    dockerfilePath: ./Dockerfile
    healthCheckPath: /
    autoDeployTrigger: checksPass
    envVars:
      - key: RV32I_GUI_HOST
        value: 0.0.0.0
